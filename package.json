{
  "private": true,
  "name": "viboplr-qbittorrent-tests",
  "description": "Test harness for the viboplr-qbittorrent plugin (not published; the plugin itself is just index.js + manifest.json).",
  "scripts": {
    "test": "node --test"
  }
}
